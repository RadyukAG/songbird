import Variants from './variants';
import { right_answer, wrong_answer, set_answer, set_round_number, set_active_bird, next_round } from './actions';

export { right_answer, wrong_answer, set_answer, set_round_number, set_active_bird, next_round };

export default Variants;
